load("@rules_oci//oci:defs.bzl", "oci_image")

oci_image(
    name = "base",
    base = "@distroless_base",
    entrypoint = ["/usr/bin/python3"],
    cmd = ["-m", "http.server"],
    tars = [
        "//:pip_deps",
    ],
)