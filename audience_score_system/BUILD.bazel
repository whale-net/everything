"""Audience Score System domain root.

No release_helm_chart yet — the domain currently has the migration job
(//audience_score_system/migrate) and a scaffold-only web binary
(//audience_score_system/web, #1570). Later M1 tasks add the mcp and worker
binaries and finish web's sign-in (see ARCHITECTURE.md); the chart composing
all four apps is wired up once they exist.
"""

filegroup(
    name = "docs",
    srcs = glob(["*.md"]),
    visibility = ["//visibility:public"],
)
