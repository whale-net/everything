load("@rules_go//go:def.bzl", "go_library")

# pgx-based repository over //libs/go/db for Audience Score System's
# identity core (migration 001: person, channel, channel_person (LB2),
# channel_invite). CanApprove/CanInvite/CanReconnect/CanRead/CanWrite
# (authz.go) are the only sanctioned M1 authorization entry points (NFR5).
#
# A Postgres-backed go_test (via //libs/go/dbtest, applying
# //audience_score_system/migrate/schema's real migrations) lands in the
# Testing phase (issue #1568).
go_library(
    name = "store",
    srcs = [
        "authz.go",
        "channel.go",
        "invite.go",
        "models.go",
        "person.go",
        "role.go",
        "store.go",
    ],
    importpath = "github.com/whale-net/everything/audience_score_system/store",
    visibility = ["//visibility:public"],
    deps = [
        "@com_github_google_uuid//:uuid",
        "@com_github_jackc_pgx_v5//:pgx",
        "@com_github_jackc_pgx_v5//pgconn",
        "@com_github_jackc_pgx_v5//pgxpool",
    ],
)
