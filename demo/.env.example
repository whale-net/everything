# Demo App Configuration
# Copy this to demo/.env and customize for your local development

# =============================================================================
# App Control
# =============================================================================

# Enable specific demo apps
DEMO_ENABLE_FASTAPI=true
DEMO_ENABLE_INTERNAL_API=false
DEMO_ENABLE_WORKER=false
DEMO_ENABLE_HELLO_PYTHON=false
DEMO_ENABLE_HELLO_GO=false
DEMO_ENABLE_JOB=false

# =============================================================================
# Infrastructure
# =============================================================================

# Enable nginx ingress controller (required for external-api apps)
DEMO_ENABLE_INGRESS=true

# =============================================================================
# Platform Configuration
# =============================================================================

# Platform for Bazel image builds
# Options: linux_arm64 (M1/M2 Macs), linux_x86_64 (Intel Macs/Linux)
TILT_PLATFORM=linux_arm64

# =============================================================================
# Development Tips
# =============================================================================
#
# Before running tilt up:
# 1. Build the helm charts:
#    bazel build //demo:fastapi_chart
#    bazel build //demo:internal_api_chart
#    bazel build //demo:worker_chart
#    bazel build //demo:job_chart
#
# 2. Or build everything at once:
#    bazel build //demo/...
#
# Access points:
# - FastAPI (external-api with ingress): http://localhost:30080
# - FastAPI (direct): http://localhost:8000
# - Internal API: http://localhost:8080 (no ingress, k8s service only)
# - Workers: Check logs with `tilt logs <worker-name>`
# - Jobs: Run on schedule or manually trigger
