@AGENTS.md

## Subagent Forking

Do not fork subagents (e.g. `Agent` with `subagent_type: "fork"`) in this
repo. Work directly in the main session instead of spawning forks for
exploratory or investigative steps.
