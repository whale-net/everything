{{/*
Migration jobs template - Generated from k8s artifacts
Sorted by hook weight for proper execution order
*/}}
{{- range .Artifacts }}
{{- if eq .Type "job" }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ $.Release.Name }}-{{ .Name }}
  labels:
    app: {{ .Name }}
    chart: {{ $.Chart.Name }}
    release: {{ $.Release.Name }}
    heritage: {{ $.Release.Service }}
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "{{ .HookWeight | default -5 }}"
    "helm.sh/hook-delete-policy": "{{ .HookDeletePolicy | default "before-hook-creation" }}"
spec:
  template:
    metadata:
      name: {{ $.Release.Name }}-{{ .Name }}
      labels:
        app: {{ .Name }}
        release: {{ $.Release.Name }}
    spec:
      containers:
      - name: {{ .Name }}
        # DEBUG: Values type: {{ typeOf $.Values }}
        # DEBUG: Has experience_api? {{ hasKey $.Values.images "experience_api" }}
        {{- if eq .Name "migrations_job" }}
        image: "{{ $.Values.domain }}/test:latest"
        command: ["python", "-m", "alembic", "upgrade", "head"]
        {{- else }}
        image: "{{ $.Values.domain }}/test:latest"
        command: ["echo", "Job {{ .Name }} executed"]
        {{- end }}
        env:
        - name: JOB_NAME
          value: {{ .Name }}
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "500m"
      restartPolicy: OnFailure
      backoffLimit: 3
{{- end }}
{{- end }}