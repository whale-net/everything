# Generated values.yaml for ${domain} domain chart
# This file is automatically generated - do not edit manually

# Image configurations for each app
images:
${image_configs}

# Domain-specific application configuration
${domain}:
  apps:
${app_configs}

# Common configuration
service:
  enabled: true
  type: ClusterIP

ingress:
  enabled: false
  host: "localhost"
  tls:
    enabled: false

env:
  app_env: "dev"
${overrides_section}