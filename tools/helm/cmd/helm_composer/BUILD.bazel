load("@rules_go//go:def.bzl", "go_binary", "go_library")

go_library(
    name = "helm_composer_lib",
    srcs = ["main.go"],
    importpath = "github.com/whale-net/everything/tools/helm/cmd/helm_composer",
    visibility = ["//visibility:private"],
    deps = ["//tools/helm:helm_lib"],
)

go_binary(
    name = "helm_composer",
    embed = [":helm_composer_lib"],
    visibility = ["//visibility:public"],
)
