# krill — TOC

- [`PRODUCT.md`](PRODUCT.md) — Vision, personas, load-bearing decisions, non-goals, and the roadmap for krill (the spec-of-record and work-tracking substrate for agent swarms). Start here. **Generated by `krill/render`, per `/AGENTS.md` § Documentation Conventions' "Generated-doc carve-out"** — this file and `product/*.md` are non-hand-editable; edit krill's entities and re-render instead of editing this file or its splits directly. See [`render/README.md`](render/README.md) for the full rule.
- [`README.md`](README.md) — What exists today (M1 domain scaffolding, issue #2487): the `migrate` and `api` binaries, local dev (`bazel run` targets + Tilt), and the plugin placeholder.
- [`render/README.md`](render/README.md) — The generated-doc carve-out in full: why `PRODUCT.md`/`product/*.md` are non-hand-editable, and how to change them correctly. Read before editing either file, or before scoping a doc-rendering change to `krill/render` itself.
- [`ARCHITECTURE.md`](ARCHITECTURE.md) — Index into `ARCHITECTURE/` (component map, then one file per topic — spec entity model, migration numbering per milestone, session/write-gate, MCP surfaces, importer/renderer, delivery axis, work axis). Read before scoping or designing any krill schema work.
- [`ENV.md`](ENV.md) — Environment variables for `migrate` and `api`.
- [`ui/README.md`](ui/README.md) — The operator web UI: the two layers on one binary (the `htmxauth` sign-in front door vs. the templ + `htmxui` shell behind it), the render seam and where the status code lives, the `HX-Request` one-route-two-modes convention, the 200-re-render error rule, the doubled-form rule, the available `htmxui` primitives and when not to re-hand-roll them, the CSS load-order trap, the templ/Bazel `go_srcs` gotcha, and the LB4 write-identity constraint. Read before adding a page, adding an htmx branch, or changing anything under `krill/ui/`.
- [`conformance/roundtrip_integration_test.go`](conformance/roundtrip_integration_test.go) — krill's self-hosting round-trip proof (issue #2497, FR18/FR19): imports krill's own committed brief through `//krill/importer`, renders it back through `//krill/render`, and asserts every FR16-reported entity is present in the output, keyed by entity id. Run with `bazel test //krill/conformance:roundtrip_integration_test`; regenerate the checked-in FR16 report artifact (`conformance/testdata/fr16_report.txt`) per that file's `TestGenerateFR16ReportArtifact` doc comment.
- [`conformance/design_milestone_query_integration_test.go`](conformance/design_milestone_query_integration_test.go) — the live-consumer half of M1's self-hosting proof (issue #2500, FR21): proves `/project-manager:design --milestone`'s krill-domain read (`slice.Querier.GetProductSlice`, FR8) surfaces a milestone's cited capability content live, and fails loud and named rather than silently empty against an unknown Product. Shares the round-trip test's Bazel target — run the same way. See `ARCHITECTURE.md` "The design skill's live milestone read (FR21)".
- [`conformance/whagent_net_import_integration_test.go`](conformance/whagent_net_import_integration_test.go) — M2's FR11 adoption proof (issue #2549): imports `whagent_net`'s real, committed brief — the first product krill holds that krill did not author — and proves the FR11 entity-id report accounts for every source item with no content lost, plus FR11's completeness-accounting (coverage) gate. Shares the round-trip test's Bazel target — run the same way. See `ARCHITECTURE.md` "Completeness accounting and the whagent_net import (M2's FR11, issue #2549)".
- [`conformance/delivery_axis_integration_test.go`](conformance/delivery_axis_integration_test.go) — M3's end-to-end delivery-axis proof (issue #2690): one connected walk through milestone authoring, milepebbles and mid-milestone discovery, status history, per-item shipment, re-cut, the backlog bucket, and abandon (issues #2683-#2689), re-asserting every captured entity/container id across the whole walk (NFR1) and re-rendering `PRODUCT.md`/`product/*.md` at the end to prove a milepebble or the backlog bucket never leaks into `product/03-roadmap.md`. Shares the round-trip test's Bazel target — run the same way. See `ARCHITECTURE.md` "M3's delivery axis, end to end (issue #2690)".
- [`conformance/work_axis_integration_test.go`](conformance/work_axis_integration_test.go) — M4's end-to-end work-axis proof (issue #2728): one connected walk through task creation, dependency gating, claim, heartbeat, lease-expiry reclaim, abandon, and complete-with-a-verdict lane advance/revert (issues #2719-#2727), then FR10's cross-host resume proof (a second session's fetch-by-id payload compared field-by-field against the first session's), then a mechanical audit of every NFR the root plan (#2717) states for this milestone (NFR1-NFR7), one subtest per NFR. Shares the round-trip test's Bazel target — run the same way, or filter to just this file's test with `--test_filter=TestWorkAxis`. See `ARCHITECTURE.md` "The work axis (M4): task, claim, lease, attempt, note".
- [`conformance/escalation_console_integration_test.go`](conformance/escalation_console_integration_test.go) — M5's end-to-end escalation/intervention/console proof (issue #2877): one connected walk through the claimed-console view, thrash-cap escalation, attempt-cap escalation, manual escalation (including FR9's single-event-at-cap exception), the escalated-console view, release, requeue (correct per-reason counter reset, notes carried through), cancel and the cancelled-console view, note lifecycle and the open-notes console view, and paging with cross-scope token rejection across the console queries (issues #2867-#2876), plus an NFR1-NFR6 and claimability-index audit. Documents, rather than silently passing or fixing, a real production defect this walk uncovered (`ListClaimedTasks`/`GET /console/claimed`/`list_claimed_tasks` is an unimplemented stub — tracked as follow-up issue #2916). Shares the round-trip test's Bazel target — run the same way, or filter to just this file's test with `--test_filter=TestEscalationConsole`. See `ARCHITECTURE.md` "The escalation/intervention/console axis (M5)".
- [`plugin/agent_tools_test.go`](plugin/agent_tools_test.go) — the plugin-tooling guard: asserts every `mcp__plugin_<plugin>_<server>__*` entry in an agent's `tools:` frontmatter names a server that the *same* plugin's manifest registers, that `.mcp.json` and `mcp_config.json` agree, and that each work-axis persona can still reach the `/mcp/work` mount carrying `claim_task`/`complete_task`/`get_task`/`record_note`. Added after those tools moved onto `/mcp/work` and the manifest servers were renamed but the `krill/plugin/work/agents/*.md` frontmatter was left untouched — an unmatched allowlist pattern is silently ignored, so five personas lost their task-lifecycle tools with nothing failing. Pure Go, no Docker: `bazel test //krill/plugin:agent_tools_test`.
