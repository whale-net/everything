# krill — TOC

- [`PRODUCT.md`](PRODUCT.md) — Vision, personas, load-bearing decisions, non-goals, and the roadmap for krill (the spec-of-record and work-tracking substrate for agent swarms). Start here. **Generated by `krill/render`, per `/AGENTS.md` § Documentation Conventions' "Generated-doc carve-out"** — this file and `product/*.md` are non-hand-editable; edit krill's entities and re-render instead of editing this file or its splits directly.
- [`README.md`](README.md) — What exists today (M1 domain scaffolding, issue #2487): the `migrate` and `api` binaries, local dev (`bazel run` targets + Tilt), and the plugin placeholder.
- [`ARCHITECTURE.md`](ARCHITECTURE.md) — Component map as of this task, the `scope` table's design rationale (LB1/LB3), and M1's migration numbering. Read before scoping or designing any krill schema work.
- [`ENV.md`](ENV.md) — Environment variables for `migrate` and `api`.
- [`conformance/roundtrip_integration_test.go`](conformance/roundtrip_integration_test.go) — krill's self-hosting round-trip proof (issue #2497, FR18/FR19): imports krill's own committed brief through `//krill/importer`, renders it back through `//krill/render`, and asserts every FR16-reported entity is present in the output, keyed by entity id. Run with `bazel test //krill/conformance:roundtrip_integration_test`; regenerate the checked-in FR16 report artifact (`conformance/testdata/fr16_report.txt`) per that file's `TestGenerateFR16ReportArtifact` doc comment.
- [`conformance/design_milestone_query_integration_test.go`](conformance/design_milestone_query_integration_test.go) — the live-consumer half of M1's self-hosting proof (issue #2500, FR21): proves `/project-manager:design --milestone`'s krill-domain read (`slice.Querier.GetProductSlice`, FR8) surfaces a milestone's cited capability content live, and fails loud and named rather than silently empty against an unknown Product. Shares the round-trip test's Bazel target — run the same way. See `ARCHITECTURE.md` "The design skill's live milestone read (FR21)".
